---
import { isAuthenticated } from '../../lib/auth';
import AdminLayout from '../../layouts/AdminLayout.astro';
import ProductManagementWrapper from '../../components/admin/products/ProductManagementWrapper';

// Check authentication first
if (!isAuthenticated(Astro.request)) {
  return Astro.redirect('/admin/login');
}
---

<AdminLayout title="Product Management - Troy BBQ">
  <ProductManagementWrapper client:load />
</AdminLayout>